[build]
builder = "nixpacks"

[deploy]
startCommand = "mix event_store.init && mix ecto.migrate && mix phx.server"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
