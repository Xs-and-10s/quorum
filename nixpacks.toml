[variables]
NIXPACKS_ELIXIR_VERSION = "1.18"
NIXPACKS_ERLANG_VERSION = "27"

[phases.build]
cmds = [
  "mix local.hex --force",
  "mix local.rebar --force",
  "mix deps.get --only prod",
  "mix compile",
  "mix assets.deploy"
]
