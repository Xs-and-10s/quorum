[phases.setup]
nixPkgs = ["elixir_1_19", "erlang_28"]

[phases.build]
cmds = [
  "mix local.hex --force",
  "mix local.rebar --force",
  "mix deps.get --only prod",
  "mix compile",
  "mix assets.deploy"
]
